# API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
python-dotenv>=1.0

# Database
supabase>=2.5
psycopg[binary]>=3.1

# LangChain (agente, chains, memory, tools)
# Série 0.3.x fixada: o projeto usa a API clássica de agents
# (create_tool_calling_agent + AgentExecutor) e RunnableWithMessageHistory,
# removidas do pacote `langchain` na série 1.x.
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-anthropic>=0.3,<0.4
langchain-postgres>=0.0.12,<0.1

# Scraping
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
tenacity>=8.3

# Google (Human-in-the-loop + Calendar)
gspread>=6.0
google-auth>=2.29
google-auth-oauthlib>=1.2
google-api-python-client>=2.130
tzdata>=2024.1  # zoneinfo no Windows
